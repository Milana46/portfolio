<section class="hero section">
  <div class="container hero-grid">
    <div class="hero-text">
      <span class="badge">Графический дизайнер</span>
      <h1>Создаю визуал, который продает и запоминается</h1>
      <p>
        Помогаю брендам упаковать идеи в сильную айдентику: от логотипа до рекламных материалов и
        соцсетей. Более 50 реализованных проектов за 1.5 года.
      </p>
      <div class="hero-actions">
        <a class="btn btn-primary" href="#works">Смотреть кейсы</a>
        <a class="btn btn-ghost" href="#contact">Заказать дизайн</a>
      </div>
      <ul class="metrics" aria-label="Ключевые метрики">
        <li><strong>50</strong><span>проектов</span></li>
        <li><strong>4.9</strong><span>средняя оценка</span></li>
        <li><strong>1.5 года</strong><span>коммерческой практики</span></li>
      </ul>
    </div>
    <div class="hero-visual card" aria-hidden="true">
      <div class="visual-badge">TOP CASES</div>
      <div class="visual-main">BRAND PACK</div>
      <div class="visual-stack">
        <span>Айдентика</span>
        <span>Упаковка</span>
        <span>Web</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: 5rem;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: 1.4rem;
    align-items: stretch;
  }

  h1 {
    margin: 1rem 0 0.8rem;
    font-size: clamp(2rem, 4.4vw, 4rem);
    line-height: 1.02;
    max-width: 14ch;
  }

  p {
    margin: 0 0 1.5rem;
    max-width: 52ch;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem;
    margin-bottom: 1.6rem;
  }

  .metrics {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.65rem;
  }

  .metrics li {
    display: flex;
    flex-direction: column;
    border: 1px solid var(--line);
    border-radius: 0.8rem;
    padding: 0.75rem;
    background: rgba(255, 255, 255, 0.02);
  }

  .metrics strong {
    font-size: 1.1rem;
  }

  .metrics span {
    color: var(--text-muted);
    font-size: 0.86rem;
  }

  .hero-visual {
    position: relative;
    overflow: hidden;
    min-height: 380px;
    display: grid;
    align-content: space-between;
    background: var(--surface);
  }

  .hero-visual::before,
  .hero-visual::after {
    content: "";
    position: absolute;
    width: clamp(240px, 42%, 360px);
    aspect-ratio: 1;
    border-radius: 50%;
    filter: blur(1px);
    pointer-events: none;
    will-change: transform;
  }

  .hero-visual::before {
    left: 8%;
    top: 10%;
    background:
      radial-gradient(circle at 32% 28%, rgba(255, 255, 255, 0.36) 0%, rgba(255, 255, 255, 0.04) 18%, rgba(255, 255, 255, 0) 28%),
      radial-gradient(circle, rgba(242, 111, 174, 0.5) 0%, rgba(242, 111, 174, 0.28) 58%, rgba(242, 111, 174, 0.08) 76%, rgba(242, 111, 174, 0) 100%);
    box-shadow:
      inset 0 0 0 1px rgba(255, 255, 255, 0.2),
      0 0 42px rgba(242, 111, 174, 0.24);
    animation: driftPink 11s ease-in-out infinite alternate;
  }

  .hero-visual::after {
    right: 7%;
    bottom: 9%;
    background:
      radial-gradient(circle at 36% 30%, rgba(255, 255, 255, 0.34) 0%, rgba(255, 255, 255, 0.05) 18%, rgba(255, 255, 255, 0) 28%),
      radial-gradient(circle, rgba(149, 216, 191, 0.46) 0%, rgba(149, 216, 191, 0.24) 58%, rgba(149, 216, 191, 0.07) 76%, rgba(149, 216, 191, 0) 100%);
    box-shadow:
      inset 0 0 0 1px rgba(255, 255, 255, 0.16),
      0 0 40px rgba(149, 216, 191, 0.2);
    animation: driftMint 14s ease-in-out infinite alternate;
  }

  .visual-badge {
    position: relative;
    z-index: 3;
    justify-self: end;
    font-size: 0.72rem;
    letter-spacing: 0.06em;
    color: var(--text-muted);
  }

  .visual-main {
    position: relative;
    z-index: 3;
    font-size: clamp(2rem, 5vw, 3.4rem);
    font-weight: 800;
    line-height: 0.95;
    max-width: 8ch;
  }

  .visual-stack {
    position: relative;
    z-index: 3;
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
  }

  .visual-stack span {
    border: 1px solid var(--line);
    border-radius: 999px;
    padding: 0.35rem 0.62rem;
    font-size: 0.78rem;
    color: var(--text-muted);
  }

  @keyframes driftPink {
    0% {
      transform: translate3d(0, 0, 0) scale(1);
    }
    50% {
      transform: translate3d(18%, 10%, 0) scale(1.1);
    }
    100% {
      transform: translate3d(28%, 4%, 0) scale(0.96);
    }
  }

  @keyframes driftMint {
    0% {
      transform: translate3d(0, 0, 0) scale(1);
    }
    50% {
      transform: translate3d(-14%, -14%, 0) scale(1.08);
    }
    100% {
      transform: translate3d(-26%, -20%, 0) scale(0.95);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-visual::before,
    .hero-visual::after {
      animation: none;
    }
  }

  @media (max-width: 900px) {
    .hero-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
